[package]
name = "vulkan-2d-triangle"
version = "0.1.0"
edition = "2024"

[dependencies]
winit = { version = "0.29", features = ["rwh_06"]}
ash = { version = "0.38.0+1.3.296", default-features = false,  features = ["linked", "debug", "std"]}
ash-window = "0.13.0"
